generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model urls {
  id           Int @id @default(autoincrement())
  shortenedURL String @unique
  url          String
}
